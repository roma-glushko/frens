import React from "react";
import ReactDOM from "react-dom/client";
import FrensApp from "./App";
import "@mantine/core/styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <FrensApp />
    </React.StrictMode>
);
